# Sample parameters for the echoserver stack.
# Replace with your domain and encrypt with `sops -e -i env/echoserver.env` for production.
ECHOSERVER_HOST=echoserver.example.invalid
ECHOSERVER_INTERNAL_PORT=80
