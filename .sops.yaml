## SOPS (age) recipients for env files
## - Replace the placeholder with your real age public key (age1...)
## - To allow multiple operators, list multiple keys: ["age1alice...", "age1bob..."]
creation_rules:
  - path_regex: env/.*\.env$
    age: ["age1YOUR_PUBLIC_KEY_HERE"]
